node_modules/
.env

# Archivos SQL (schema, seeds, scripts de mantenimiento) — todo ya
# vive aplicado en Neon, no tiene sentido versionar volcados locales
# en un repo público. Se guardan solo como respaldo en la máquina.
*.sql
DATOS_NO_COMPATIBLES.md
