node_modules/
.env

# Volcados de datos reales (PII de clientes/proveedores, hashes de
# contraseñas) — el repo es público, nunca deben subirse.
# El schema y los datos ya viven en Neon; estos archivos son solo
# respaldo/herramienta local.
bd.sql
seed_datos_reales.sql
DATOS_NO_COMPATIBLES.md
